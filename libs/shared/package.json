{
  "name": "@nx-mono-repo-deployment-test/shared",
  "version": "1.0.0",
  "description": "Shared utilities and types",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
